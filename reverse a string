#include<stdio.h>
void main(){
    int n,r=0,a,b; 
    printf("Enter the number to reverse: ");
    scanf("%d",&n);
    a=n;
    for(;n>0;){
        b=n%10;
        r=r*10+b;
        n=n/10;
    }
    printf("Reverse of %d is %d",a,r);
}
